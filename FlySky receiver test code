/*
SpiderBot FlySky Receiver Test
Reads a receiver channel and prints the value.
*/

const int CH1 = 3;

void setup() {
Serial.begin(9600);
pinMode(CH1, INPUT);
}

void loop() {
int value = pulseIn(CH1, HIGH, 25000);

Serial.print(“CH1: “);
Serial.println(value);

delay(100);
}